# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
*.swp
*.swo
*~
.vscode/
.idea/

# Node (if ever added)
node_modules/

# Python
__pycache__/
*.pyc

# Secrets / credentials
.env
.env.*
*secret*.json
*token*.json
credentials.json
*service_account*.json
*.key
*.pem

.tmp.driveupload/
.claude/

# Video / audio (should never be in public repo)
*.mp4
*.mp3
*.wav
*.avi
*.mkv
*.mov
*.webm

# Office documents (source material, not web content)
*.pptx
*.docx
*.xlsx

# Archives (textbook ZIPs are in downloads/ — tracked intentionally)
# *.zip

# Large image formats (PNGs in infographics/ are tracked intentionally)
*.psd
*.tiff
*.bmp
*.raw
